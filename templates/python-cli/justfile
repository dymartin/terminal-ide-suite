default:
    @just --list

dev:
    uv run python -m app

run: dev

test:
    uv run pytest -q

test-watch:
    watchexec -r -e py -- uv run pytest -q

lint:
    uv run ruff check .

fmt:
    uv run ruff format .

build:
    uv build
